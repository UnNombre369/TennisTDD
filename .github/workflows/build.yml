name: SonarQube

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  build:
    name: Build, test and SonarQube analysis
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: TennisND

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'zulu'

      - name: Cache SonarQube packages
        uses: actions/cache@v4
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      - name: Cache Maven repository
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      - name: Build, run tests and generate JaCoCo report
        run: |
          mvn -B -V org.jacoco:jacoco-maven-plugin:0.8.10:prepare-agent verify org.jacoco:jacoco-maven-plugin:0.8.10:report

      - name: Upload test and coverage reports
        uses: actions/upload-artifact@v4
        with:
          name: reports
          path: |
            TennisND/target/site/jacoco
            TennisND/target/surefire-reports

      - name: SonarQube analysis
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        run: |
          set -e
          ARGS=("-Dsonar.projectKey=UnNombre369_TennisTDD" "-Dsonar.login=$SONAR_TOKEN")
          if [ -n "$SONAR_HOST_URL" ]; then
            ARGS+=("-Dsonar.host.url=$SONAR_HOST_URL")
          fi
          mvn -B org.sonarsource.scanner.maven:sonar-maven-plugin:sonar "${ARGS[@]}"

      - name: "Optional: show artifact list"
        run: |
          echo "Uploaded reports (if any):"
          ls -la TennisND/target/site || true
          ls -la TennisND/target/surefire-reports || true
